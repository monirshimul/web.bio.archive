import Headings from "../../../ui/components/Headings";

const page = () => {
  return (
    <div>
      <Headings
        title={
          "This page will show, how different input type can be handle through useReducers hook"
        }
      />
    </div>
  );
};

export default page;
